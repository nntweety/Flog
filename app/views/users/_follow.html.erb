<% if current_user%>
 <% if current_user.following?(user) %>
  <%= link_to "フォロー中", user_follows_path(user.id), method: :delete, class:"btn btn-primary", remote: true %>
 <% else %>
  <%= link_to "フォローする", user_follows_path(user.id), method: :post, class:"btn btn-light", remote: true %>
 <% end %>
 <% else %>
<% end %>